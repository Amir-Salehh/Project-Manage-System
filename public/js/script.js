    function viewProject(id) {
    // Here, you can add a dynamic fetching mechanism for project details based on the ID.
    document.getElementById('projectDetails').innerText = 'جزئیات پروژه شماره ' + id + ' نمایش داده می‌شود.';
}
